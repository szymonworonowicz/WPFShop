<Page x:Class="WpfProject.Pages.UserPages.Orders"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfProject.Pages.UserPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Orders" Background="White"
      Loaded="OrdersLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Margin="5" HorizontalAlignment="Left"> Twoje Zamowienia</Label>
        
        <ListView Name="OrderList" Margin="5" Grid.Row="1" Grid.ColumnSpan="2">

            <ListView.View>
                <GridView AllowsColumnReorder="False" >
                    <GridViewColumn DisplayMemberBinding="{Binding Date}" Header="Data Zamowienia" Width="auto" ></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Status}" Header="Status" Width="auto" ></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Amount}" Header="Kwota" Width="auto" ></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
    
</Page>
