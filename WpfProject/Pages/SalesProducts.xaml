<Page x:Class="WpfProject.Pages.SalesProducts"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfProject.Pages"
      xmlns:converters ="clr-namespace:WpfProject.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SalesProducts"
      Loaded="LoadSaleProducts">
    <Page.Resources>
        <converters:ImageConverter x:Key="ImageConverter"/>
        <DataTemplate x:Key="SaleTemplate">
            <Border x:Name="Border" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <Grid Margin="3" x:Name="Grid" Grid.IsSharedSizeScope="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition x:Name="Row1" Height="*"></RowDefinition>
                        <RowDefinition x:Name="Row2" Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <Label Grid.Column="0" Grid.Row="0" Content="{Binding Name}"></Label>
                    <Label Grid.Column="0" Grid.Row="1" Content="{Binding Price}"></Label>
                    <Image Grid.Column="1" Grid.RowSpan="2"  MinHeight="50" MinWidth="50" MaxHeight="250" MaxWidth="250"  Source="{Binding Path=Photo, Converter={StaticResource ImageConverter}}"></Image>
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>
    <ListBox Name="SalesProduct" Grid.Row="3" Grid.ColumnSpan="4" Margin="5" Padding="5" BorderThickness="0" ItemTemplate="{StaticResource SaleTemplate}">
        <ListBox.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid x:Name="Uniform" Columns="3" Rows="2" VerticalAlignment="Stretch"  HorizontalAlignment="Stretch" Margin="5"></UniformGrid>
            </ItemsPanelTemplate>
        </ListBox.ItemsPanel>
    </ListBox>
</Page>
